# Vue SPA Environment Configuration

# Application
VITE_APP_NAME="Vue Slide Demo"
VITE_APP_URL=http://localhost:5173

# Storage
VITE_STORAGE_PREFIX=vsd

# Data Source Mode
# Options: local, api, hybrid
# - local: All data stored in browser (LocalStorage + IndexedDB)
# - api: All data fetched from REST API backend (requires VITE_API_URL)
# - hybrid: Offline-first with API sync (future implementation)
VITE_DATA_SOURCE=local

# API Configuration (required for api and hybrid modes)
# Set this to your backend API base URL (without /api suffix)
# Local development: https://api.local.test or http://localhost:8000
# Production: https://api.your-domain.com
VITE_API_URL=

# Debug Mode
# Set to true to enable additional logging
VITE_DEBUG=false
